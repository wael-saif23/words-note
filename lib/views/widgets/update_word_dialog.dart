import 'package:flutter/material.dart';

class UpdateWordDialog extends StatelessWidget {
  const UpdateWordDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return const Dialog();
  }
}